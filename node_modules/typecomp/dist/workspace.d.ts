import { Diagnostic } from "./diagnostic";
import { Output } from "./output";
export declare class Workspace {
    private readonly projects;
    private readonly registry;
    constructor();
    private projectFor(filename);
    index(filename: string): void;
    diagnose(filename: string): Array<Diagnostic>;
    compile(filename: string): Array<Output>;
}
